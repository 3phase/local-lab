import type {Dataset} from './datasetTypes';
export function parseCsvOffThread(input:File|string,source:Dataset['source']='upload',name?:string,onProgress?:(progress:number,label:string)=>void):Promise<Dataset>{if(typeof Worker==='undefined')return import('./parseCsv').then(({parseCsv})=>parseCsv(input,source,name));return new Promise((resolve,reject)=>{const worker=new Worker(new URL('./csvWorker.ts',import.meta.url),{type:'module'});worker.onmessage=(event:MessageEvent<{type:string;progress?:number;label?:string;dataset?:Dataset;message?:string}>)=>{if(event.data.type==='progress')onProgress?.(event.data.progress??0,event.data.label??'Processing');if(event.data.type==='complete'){worker.terminate();resolve(event.data.dataset!)}if(event.data.type==='error'){worker.terminate();reject(new Error(event.data.message))}};worker.onerror=event=>{worker.terminate();reject(new Error(event.message||'Dataset worker failed.'))};worker.postMessage({input,source,name})})}
