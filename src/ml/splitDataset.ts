export function seededRandom(seed:number){return()=>{seed|=0;seed=seed+0x6D2B79F5|0;let t=Math.imul(seed^seed>>>15,1|seed);t=t+Math.imul(t^t>>>7,61|t)^t;return((t^t>>>14)>>>0)/4294967296}}
export function splitIndices(labels:(string|number)[],validation=.2,seed=42,stratified=true){const rng=seededRandom(seed);const shuffle=(a:number[])=>{for(let i=a.length-1;i>0;i--){const j=Math.floor(rng()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a};let groups:number[][];if(stratified){const m=new Map<string,number[]>();labels.forEach((l,i)=>m.set(String(l),[...(m.get(String(l))??[]),i]));groups=[...m.values()]}else groups=[labels.map((_,i)=>i)];const train:number[]=[];const val:number[]=[];for(const g of groups){shuffle(g);const n=Math.max(g.length>1?1:0,Math.round(g.length*validation));val.push(...g.slice(0,n));train.push(...g.slice(n))}return{train:shuffle(train),validation:shuffle(val)}};
