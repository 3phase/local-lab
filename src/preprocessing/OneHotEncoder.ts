export type OneHotEncoderState={type:'OneHotEncoder';column:string;maxVocabSize:number;labels:string[];labelToIndex:Record<string,number>;includeOtherBucket:boolean};
export const fitOneHot=(column:string,values:unknown[],maxVocabSize=20):OneHotEncoderState=>{const counts=new Map<string,number>();values.forEach(v=>{const k=v==null?'__MISSING__':String(v);counts.set(k,(counts.get(k)??0)+1)});let labels=[...counts].sort((a,b)=>b[1]-a[1]).slice(0,maxVocabSize).map(x=>x[0]);const includeOtherBucket=counts.size>labels.length;if(includeOtherBucket)labels.push('__OTHER__');return{type:'OneHotEncoder',column,maxVocabSize,labels,labelToIndex:Object.fromEntries(labels.map((x,i)=>[x,i])),includeOtherBucket}};
export const oneHot=(v:unknown,s:OneHotEncoderState)=>{const out=Array(s.labels.length).fill(0);let k=v==null?'__MISSING__':String(v);if(s.labelToIndex[k]===undefined&&s.includeOtherBucket)k='__OTHER__';if(s.labelToIndex[k]!==undefined)out[s.labelToIndex[k]]=1;return out};
